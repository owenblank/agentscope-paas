# AgentScope-PaaS 框架核心依赖
# Python >= 3.8, < 4.0
# 使用说明: pip install -r requirements.txt

# ============================================
# 核心依赖（必需）
# ============================================
# AgentScope核心库（版本约束：1.0.x系列，确保兼容性）
agentscope~=1.0.19

# YAML配置文件解析（6.x系列，避免7.x breaking changes）
pyyaml>=6.0,<7.0

# 日期时间处理（用于时间戳和日期解析）
python-dateutil>=2.8.0,<3.0

# HTTP请求库（用于知识库API调用和外部服务集成）
requests>=2.28.0,<3.0

# ============================================
# 可选依赖（推荐安装）
# ============================================
# 环境变量管理（从.env文件加载配置）
python-dotenv>=1.0.0,<2.0

# 类型检查支持（提供向后兼容的类型提示）
typing-extensions>=4.4.0,<5.0

# ============================================
# Web API 服务器依赖
# ============================================
# FastAPI框架（高性能Web API）
fastapi>=0.104.0,<1.0

# ASGI服务器（运行FastAPI应用）
uvicorn[standard]>=0.24.0,<1.0

# 数据验证和序列化（FastAPI依赖）
pydantic>=2.0.0,<3.0